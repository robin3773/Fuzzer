# Example profile favouring schema-heavy mutations.
strategy: HYBRID
verbose: true
enable_c: true

probabilities:
  decode: 90
  imm_random: 15

weights:
  r_base_alu: 85
  r_m: 50

schemas:
  isa: rv32i
  root: ./schemas
  map: isa_map.yaml