# Default runtime settings mirroring historical in-code behaviour.
strategy: IR
verbose: false
enable_c: false

probabilities:
  decode: 60
  imm_random: 25

weights:
  r_base_alu: 70
  r_m: 30

schemas:
  isa: rv32imc
  root: ./schemas
  map: isa_map.yaml